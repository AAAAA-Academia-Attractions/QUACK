game:
  num_players: 6
  num_ducks: 1
  max_ticks: 200
  map: configs/maps/simple_ship.yaml

tasks:
  ticks_per_task: 3
  tasks_per_player: 5

kill:
  cooldown_ticks: 5
  initial_cooldown: 5

meeting:
  max_discussion_rounds: 2
  emergency_meetings_per_player: 1

vision:
  visibility_range: 0  # 0 = current room only (same room vision)
  fog_memory_ticks: 0  # instantaneous vision only, no fog memory
