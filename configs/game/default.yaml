num_players: 6
num_ducks: 1
max_ticks: 200

tasks:
  ticks_per_task: 3
  tasks_per_player: 5

kill:
  cooldown_ticks: 5
  initial_cooldown: 5

meeting:
  max_discussion_rounds: 2

vision:
  visibility_range: 0
  fog_memory_ticks: 0
